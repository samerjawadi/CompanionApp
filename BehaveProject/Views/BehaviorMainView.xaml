<UserControl x:Class="BehaveProject.Views.BehaviorMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"   
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf" 
            
             xmlns:iconpack="http://metro.mahapps.com/winfx/xaml/iconpacks"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid Background="#192428" Margin="10 10 0 5" >
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>



        <syncfusion:Carousel  Grid.Row="1" 
                Cursor="Hand"
                EnableRotationAnimation="True"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                x:Name="carousel"
                ItemsPerPage="3"
                EnableLooping="true"
                ItemsSource="{Binding Modes}"
                OpacityFraction="0"
                VisualMode="CustomPath"
                              
                              
                RotationSpeed="1">
            <syncfusion:Carousel.Path>
                <Path Data="M0,300 L600,300"/>
            </syncfusion:Carousel.Path>
            <syncfusion:Carousel.ItemTemplate>
                <DataTemplate>
                    <Border Cursor="Hand" Margin="-600 0"  VerticalAlignment="Stretch" HorizontalAlignment="Stretch">

                        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                            <TextBlock Grid.Row="0" Text="{Binding Name}" TextWrapping="Wrap" TextAlignment="Left"
                                                Foreground="White"  
                                                FontSize="30" VerticalAlignment="Center"
                                                HorizontalAlignment="Left" FontWeight="DemiBold"/>

                            <Border  MinWidth="1200" MinHeight="700" CornerRadius="15"  >
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#414c50"/>
                                        <Setter Property="BorderBrush" Value="{Binding Color}"/>
                                        <Setter Property="BorderThickness" Value="4"/>

                                    </Style>
                                </Border.Style>

                            </Border>
                        </StackPanel>


                    </Border>



                </DataTemplate>
            </syncfusion:Carousel.ItemTemplate>
        </syncfusion:Carousel>
        
        <Button VerticalAlignment="Top" Grid.Row="0" Grid.Column="1" Command="{Binding CloseViewCommand}" Cursor="Hand" Margin="10 0 30 0" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0">
            <iconpack:PackIconCoolicons Kind="House01" Foreground="Gray" Width="30" Height="30" />
        </Button>
        
    </Grid>
</UserControl>
 