<UserControl x:Class="BehaveProject.Views.BehaviorMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"   
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:iconpack="http://metro.mahapps.com/winfx/xaml/iconpacks"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid Background="#192428" Margin="10 10 0 5" >
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <ItemsControl Grid.Row="2"  ItemsSource="{Binding Modes}" HorizontalAlignment="center">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border   Cursor="Hand"
                                         Margin="5"
                                        CornerRadius="15" 
                                        VerticalAlignment="Bottom">

                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="#414c50"/>
                                <Setter Property="Height" Value="100"/>

                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSelected}" Value="true">
                                        <Setter Property="BorderBrush" Value="{Binding Color}"/>
                                        <Setter Property="BorderThickness" Value="4"/>
                                        <Setter Property="Height" Value="105"/>
                                        <Setter Property="Width" Value="200"/>

                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsSelected}" Value="false">
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="BorderBrush" Value="White"/>
                                        <Setter Property="Height" Value="100"/>
                                        <Setter Property="Width" Value="200"/>

                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <i:Interaction.Triggers>

                            <i:EventTrigger EventName="MouseLeftButtonDown">
                                <i:InvokeCommandAction Command="{Binding SelectedCommand}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="1*"/>
                                <RowDefinition Height="auto"/>

                            </Grid.RowDefinitions>


                            <TextBlock Grid.Row="0" Text="{Binding Name}" TextWrapping="Wrap" TextAlignment="center"
                                                   Foreground="White"  
                                                   FontSize="25" VerticalAlignment="Center"
                                                   HorizontalAlignment="Center" FontWeight="DemiBold"
                                                   />
                            <Border Grid.Row="1"   CornerRadius="5" Margin="15 5 15 15">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#414c50"/>
                                        <Setter Property="Height" Value="0"/>

                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSelected}" Value="true">
                                                <Setter Property="Background" Value="{Binding Color}"/>
                                                <Setter Property="Height" Value="10"/>
                                            </DataTrigger>

                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                            </Border>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Border Grid.Row="1" Margin="30 10 30 10"  BorderBrush="{Binding SelecetdMode.Color}"  BorderThickness="4" CornerRadius="10" >

        </Border>
        <Button VerticalAlignment="Top" Grid.Row="0" Grid.Column="1" Command="{Binding CloseViewCommand}" Cursor="Hand" Margin="10 0 10 0" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0">
            <iconpack:PackIconCoolicons Kind="House01" Foreground="Gray" Width="25" Height="25" />
        </Button>
    </Grid>
</UserControl>
